name: build-megatron

on:
  push:
    branches: [main, wpb_byteps]
    paths:
      - "Dockerfile"
      - "Megatron-LM/**"
      - "apex/**"
      - "vocab/**"
      - "uvinstall.sh"
      - ".github/workflows/build-megatron.yml"
  workflow_dispatch:

jobs:
  build-and-push-megatron:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      IMAGE_MEGATRON: ghcr.io/${{ github.repository_owner }}/byteps-megatron
      IMAGE_BYTEPS: ghcr.io/${{ github.repository_owner }}/byteps
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android /usr/local/share/powershell /opt/hostedtoolcache || true
          docker system prune -af || true
          df -h

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (megatron)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: megatron
          build-args: |
            BYTEPS_IMAGE=${{ env.IMAGE_BYTEPS }}:latest
          push: true
          tags: |
            ${{ env.IMAGE_MEGATRON }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
