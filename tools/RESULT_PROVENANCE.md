# Result Provenance

This file maps the current `docs/benchmark_runs/` result directory families to
tracked scripts under `tools/`. Benchmark output directories remain ignored; the
scripts and this provenance map are the reproducible artifacts to keep in git.

Audit date: 2026-06-29.

| Result directory pattern | Count observed | Script / status | Keep decision |
| --- | ---: | --- | --- |
| `*-megatron-host-vs-dpu-dataaddr/` | 95 | `tools/run_megatron_ucx_semantic.py` | Keep. This is the current NCCL / Host-PS / DPU-PS Megatron runner and covers the latest DP Qwen-3B, TP Llama2-7B, and DPTP Qwen3-4B checks. |
| `*-paper-revision-megatron/` | 18 | `tools/run_paper_revision_megatron.sh` | Keep. This is the main paper Megatron matrix runner for DP / TP / TP+DP, NCCL vs Host-PS, server count, partition, overlap, and long loss runs. |
| `*-dp-tp-dptp-15/` | 3 | Historical predecessor of `tools/run_paper_revision_megatron.sh` | Keep the current paper revision runner as the replacement. These results use the same DP / TP / DPTP matrix shape but predate the current `paper-revision-megatron` batch name. |
| `*-paper-extra/` | 1 | `tools/run_paper_extra_benchmarks.sh` | Keep as historical exact provenance for the smaller server-count, partition, and overlap ablation. |
| `*-tp-only-ucx-sweep/` | 2 | `tools/run_tp_only_ucx_sweep.sh` | Keep. This covers the TP-only UCX parameter sweep used for tuned TP-only rows. |
| `*-byteps-payload-sweep-8w8s/` | 3 | `tools/run_byteps_payload_sweep.sh` | Keep. This covers the BytePS push-pull payload-size sweep. |
| `*-byteps-bandwidth-8w8s/` | 1 | Historical one-off bandwidth sanity run | Keep the result as historical evidence only. The maintained paper bandwidth runner is `tools/run_byteps_payload_sweep.sh`. |
| `*-vgg16-ddp-8w/` | 1 | `tools/run_vgg16_ddp_benchmark.sh` | Keep. This covers the VGG16 DDP baseline. |
| `*-byteps-ucx-8w8s/` | 1 | Current equivalent: `tools/run_vgg16_byteps_benchmark.sh` | Keep. The old result was produced from the same BytePS VGG16 worker/server path; the tracked runner now emits the clearer `*-vgg16-byteps-8w8s/` batch name. |
| `*-partition-8m-*` | 5 | Paper-revision-format one-off reruns | Keep `tools/run_paper_revision_megatron.sh` as the maintained source. These directories are one-row/subset reruns of the paper revision partition path, not a separate current runner. |
| `mega-hostsched*-no-semantic/` | 4 | Covered by `tools/run_megatron_ucx_semantic.py` schedule knobs | Keep the current semantic runner. The historical directories are Host-PS schedule ablations using `BYTEPS_SERVER_ENABLE_SCHEDULE=0/1`; the current runner exposes that variable and records it in summaries. |
| `*-byteps-host-vs-dpu/` | 1 | Historical standalone BytePS host-server vs DPU-server comparison | No current first-class runner. Keep the result as historical evidence, but do not claim it is generated by an existing `tools/` script. |
| `dpu*`, `host8s8w*` bring-up directories | 14 | Historical DPU/host BytePS bring-up logs | No current first-class runner. Keep as debug evidence only. |
| `*-fresh-fused-image-sync/` | 1 | Image/container synchronization inventory | Not a benchmark runner output. Keep as operational evidence only. |

Conclusion:

- Every tracked `tools/` runner maps to at least one benchmark result family or
  to the current Megatron operational runbook.
- Some old result directories are intentionally documented as historical or
  bring-up evidence rather than being forced onto a current script.
- Do not delete a runner only because a newer runner supersedes it; the older
  script may still be the exact provenance for an old result table.
